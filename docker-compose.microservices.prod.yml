services:
  ###############################################################################
  # Redis
  ###############################################################################
  # redis:
  #   logging:
  #     driver: 'syslog'
  #     options:
  #       syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
  #       tag: '{{.Name}}/{{.ID}}'
  #     #   syslog-facility: 'daemon'
  #   depends_on:
  #     - kibana
  ###############################################################################
  # Mongo
  ###############################################################################
  # mongo:
  #   logging:
  #     driver: 'syslog'
  #     options:
  #       syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
  #       tag: '{{.Name}}/{{.ID}}'
  #     #   syslog-facility: 'daemon'
  #   depends_on:
  #     - kibana
  ###############################################################################
  # Redis & Mongo: data services that must auto-recover from crashes
  # in production. Without `restart: unless-stopped`, a transient OOM or
  # disk-full event leaves the entire stack in a failed state because
  # every app container depends on these two.
  ###############################################################################
  redis:
    restart: unless-stopped
  mongo:
    restart: unless-stopped
  ###############################################################################
  # Gateway
  ###############################################################################
  gateway:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: services/gateway/Dockerfile
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.5'
  ###############################################################################
  # Authenticator
  ###############################################################################
  authenticator:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: services/authenticator/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.5'
  ###############################################################################
  # PDFGenerator
  ###############################################################################
  pdfgenerator:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: services/pdfgenerator/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.5'
  ###############################################################################
  # voiceasr
  ###############################################################################
  voiceasr:
    restart: unless-stopped
    build:
      context: .
      dockerfile: services/voiceasr/Dockerfile
    deploy:
      resources:
        limits:
          # Measured on the J4125: ~540MB steady (model session) + ~400MB peak
          # per decode at the 15s span cap. 1536M holds the worst case with
          # headroom; the span cap in vadseg.py is what makes this bound REAL
          # (an uncapped 86s span measured 6.2GB and would OOM anything sane).
          memory: 1536M
          cpus: '3'
  ###############################################################################
  # Emailer
  ###############################################################################
  emailer:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: services/emailer/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.5'
  ###############################################################################
  # api
  ###############################################################################
  api:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: services/api/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '1.0'
  ###############################################################################
  # tenantapi
  ###############################################################################
  tenantapi:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: services/tenantapi/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 500M
          cpus: '0.5'
  ###############################################################################
  # Landlord frontend
  ###############################################################################
  landlord-frontend:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: webapps/landlord/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
  ###############################################################################
  # Tenant frontend
  ###############################################################################
  tenant-frontend:
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: webapps/tenant/Dockerfile
    # logging:
    #   driver: syslog
    #   options:
    #     syslog-address: 'udp://localhost:$LOGSTASH_UDP_PORT'
    #     tag: '{{.Name}}/{{.ID}}'
    # depends_on:
    #   - kibana
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
